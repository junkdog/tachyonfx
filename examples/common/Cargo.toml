[package]
name = "common"
version = "0.1.0"
edition = "2021"

[dependencies]
tachyonfx = { path = "../.." }
ratatui = { version = "0.30.0-alpha.5", default-features = false, features = ["std", "crossterm"] }
crossterm = "0.29.0"
bon = { version = "3.7.0", default-features = false }